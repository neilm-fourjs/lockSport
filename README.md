A simple lockSport example program

For PostgreSQL
```
sudo -u postgres createdb locksport
sudo -u postgres psql
psql (9.6.7)
Type "help" for help.

postgres=# grant all privileges on database locksport to neilm;
GRANT
postgres=# exit
